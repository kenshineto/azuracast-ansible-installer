# AzuraCast Ansible Installation

This is my fork of the unmaintained ansible installer for azuracast.

- This installer will currently only run on debian trixie (13).

## Installing

```bash
git clone https://github.com/kenshineto/azuracast-ansible-installer.git .
bash install.sh
```

## Updating

```bash
bash update.sh
```
